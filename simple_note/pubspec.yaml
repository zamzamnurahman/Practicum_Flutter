name: simple_note
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^7.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^0.13.6
  intl: ^0.18.1
  path: any
  sqflite: ^2.2.8+1

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true
